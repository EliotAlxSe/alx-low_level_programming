#include "main.h"

/**
 * _strcat - a pointer function that concatenates two strings
 * @dest: String to be concatinated
 * @src: Second string
 *
 * Return: A concatinated string
 */

char *_strcat(char *dest, char *src)
{
	int = -1, i;
	for (i = 0; dest[i] != '\0'; i++)
	do 
	{
		a++;
		dest[i] = src[a];
		i++;
	}while (src[a] != '\0');

	return (dest);
}
