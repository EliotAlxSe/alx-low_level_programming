#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 * @s: charecter poiner(A string to be converted to a length)
 * Return: The length of a pointer @c
 */
int _strlen(char *s)
{
return (strlen(s));
}
